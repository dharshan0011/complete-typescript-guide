"use strict";
const drink = {
    color: 'brown',
    carbonated: true,
    sugar: 40,
};
//tuple with consistent order
const pepsi = ['brown', true, 40];
const sprite = ['clear', true, 30];
// pepsi[0] = true
// Tuple vs Object
const carSpecs = [500, 2245];
// much more meaningful than a tuple
const carStats = {
    horsepower: 500,
    weight: 2245,
};
